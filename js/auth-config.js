window.AICreativeLabAuthConfig = Object.freeze({
  // Supabase Dashboard -> Project Settings -> API
  // Only use the public project URL and publishable/anon key here.
  // Never place a service_role key in a browser file.
  supabaseUrl: "",
  supabasePublishableKey: "",
});
